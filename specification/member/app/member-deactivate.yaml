delete:
  tags:
    - Member
  summary: 회원 탈퇴
  description: |
    회원 탈퇴를 요청합니다.

  parameters:
    - $ref: "../../fms-token.yaml"

  responses:
    "200":
      description: "성공"
      content:
        "application/json":
          schema:
            properties:
              common:
                $ref: "../../common-response.yaml"
